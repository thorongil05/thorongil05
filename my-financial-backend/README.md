# README

## Local Setup

1. Run command `npm install` to install all the required dependency
2. Run command `node app` to run the application
3. Go to localhost:3000/status to check if the app is `running`
